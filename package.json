{
  "name": "a11y-widget-v1",
  "version": "1.0.0",
  "description": "Accessibility Widget v1 - WCAG 2.1 AA-aligned support layer",
  "type": "module",
  "main": "a11y-widget.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "preinstall": "npm install --prefix website || true",
    "sync-widget": "node scripts/sync-widget-to-npm.js",
    "install:all": "npm install && npm install --prefix website",
    "build": "npm run sync-widget && npm run install:all && npm run build --prefix website",
    "prebuild": "npm run sync-widget",
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "@careerdriver/a11y-widget": "file:./packages/a11y-widget",
    "@neondatabase/serverless": "^0.9.0",
    "bcryptjs": "^2.4.3",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2"
  },
  "engines": {
    "node": ">=18.x"
  },
  "keywords": [
    "accessibility",
    "a11y",
    "wcag",
    "widget"
  ],
  "author": "",
  "license": "MIT"
}
